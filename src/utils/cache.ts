// utils/cache.ts — CACHE DISABLED
console.log("CACHE IS DISABLED — AI check runs every time");

export const getCachedAI = async (): Promise<null> => {
  return null;
};

export const setCachedAI = async (): Promise<void> => {
  // No-op
};

export const invalidateAI = async (): Promise<void> => {
  // No-op
};